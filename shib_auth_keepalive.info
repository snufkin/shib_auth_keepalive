name = Shibboleth Auth keep-alive
description = Helper module for Shiboleth to allow for sessions to persists even if the user loses the SSO cookie.

dependencies[] = shib_auth

core = 6.x

package = Shibboleth
